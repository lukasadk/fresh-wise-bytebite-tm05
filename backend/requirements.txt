fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
greenlet==3.1.1
asyncpg==0.30.0
psycopg[binary]==3.2.3
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
alembic==1.14.0
apscheduler==3.10.4
# python:3.12.7-slim (Dockerfile) has no system IANA tz database, so
# zoneinfo.ZoneInfo(tz) in routers/dashboard.py's monthly_report() raises
# ZoneInfoNotFoundError for any real zone (e.g. "Asia/Kuala_Lumpur") even
# though "UTC" happens to work without it. This is the documented fallback
# per PEP 615 -- see https://docs.python.org/3/library/zoneinfo.html.
tzdata==2026.2

# reference-data ingestion scripts
pandas==2.2.3

# tests
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1